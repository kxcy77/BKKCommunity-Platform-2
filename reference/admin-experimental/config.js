// A public API URL is configuration, not a secret. Keep this empty when the
// dashboard is served behind the same /api/v1 reverse proxy as the API.
window.BKK_API_BASE_URL = window.BKK_API_BASE_URL || ''
